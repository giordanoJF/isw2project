\subsection{Milestone 4 -- Refactoring automatizzato}

\subsubsection{Selezione delle classi}

PMD su \texttt{4.1.1}, dopo i tre filtri (LOC $\geq 100$, rimozione di interfacce e
classi astratte, Nsmells $\geq 10$), produce \textbf{602 classi} concrete ordinate per
Nsmells decrescente. Le due classi selezionate sono
riportate in Tabella~\ref{tab:m4-selected}.


\subsubsection{Refactoring e analisi (C\textsubscript{1}--C\textsubscript{4})}

% TODO: compilare dopo aver prodotto C_1--C_4 e scritto i test.
% Per ogni versione riportare:
%   - compila? (da solo e con il sistema)
%   - smell residui: quanti, nuovi o vecchi?
%   - feature bugginess-positive più alte in C_X rispetto a C_0? (indicatore negativo)
%   - feature bugginess-negative più alte in C_X rispetto a C_0? (indicatore positivo)
