\subsection{Milestone 4 -- Refactoring automatizzato}

\subsubsection{Selezione delle classi}

PMD su \texttt{4.1.1}, dopo filtro LOC $\geq 100$ e rimozione di interfacce e classi
astratte, produce \textbf{623 classi} concrete ordinate per Nsmells decrescente
(pareggi: LOC decrescente). Con $X=2$, le due classi selezionate (rank~3 e rank~621)
sono riportate in Tabella~\ref{tab:m4-selected}.

% TODO: refactoring C_1-C_4 -- compilazione, smell residui, confronto feature bugginess.
