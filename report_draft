drop ticket with created date before any version (released) of the project
drop version released

i cannot pass results in Main because the check needs to be built N times (once per project),
 not once globally. The factory pattern is the mechanism that delays construction to the right moment.

A pure orchestrator would just coordinate calls to other classes without doing any filtering logic itself.
 I could extract a IssueFilterService and VersionFilterService.
  The filtering logic is two stream lines, not complex enough to justify its own class.
    An orchestrator becomes a service candidate when its internal logic grows complex enough to need its own tests
     or has its own reason to change independently.
       It is an orchestrator with a small amount of unavoidable logic, which is acceptable.
       The moment the filtering logic grows i can extract a FilterService and restore the pure orchestrator role.


probably there is a little of redundancy between checks and enrichers