sonarcube zero code smells
understand better the consistency checks
better terminal colors
crosslin versions and dates
remove idea folder
services should be static or not?
enricher should create a copy of issues
check logic of enricher according to enrich only with opening version for now
create the csv according to check if all the version are setted
check if run works
some tickets have not OV because VersionDateService can't find one
filters with the config file
consistency rename to filter
more package to show orchestrator and services
drop ticket with created date before any version released of the project
delete data structure of tickets and version after creating csv
move logs from main to services
enricher will do eventually av prediction
make a check on fixed version
check csv logic
reuse VersionDateService method for the IssueCreatedAfterOldestVersionCheck
use the same datetime inside csv
hope that you want the same consistency check for the project used also for cold start

too complex orchestrator consistency, not reuseing method for oldest version, inside main strange check args

fixed after opening check
implement check just for some projects
check if fix is after equal oldest project version (should be included in other check because if
    fixed is after opening, and opening is after o equal oldest project version, then the same for fixed

if multiple fixed? -> take the most recent one

fix after opening check seems to be not working

make some static classes

will it not works if i enrich before checks? i say so because i neet to ernich before according to check on the Opening version

-----------------------

enrich with one fixed should map also the other fixed ( if more than one) to AV (only if av are not present)
think about removing the hard check on the opening version strictly >= oldest version, same for fixed ( will remove a lot)
start the report
check also keep and paper notebook
add a counter for each check how many issues are removed